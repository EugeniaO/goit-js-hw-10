const selectElement = document.querySelector('#breed-select');
const catInfoElement = document.querySelector('.cat-info');
const loaderBoxElement = document.querySelector('.loader-box');

export default { 
    selectElement,
    catInfoElement,
    loaderBoxElement
};